int sansaXor(vector<int> arr) {
    int result = 0;
    
    if(arr.size() % 2 != 0){
        for (int i = 0; i < arr.size(); i+=2) {
            result ^=arr[i];
        
        }
    }
    return result;
}
